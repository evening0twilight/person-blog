● 技术栈：vue3+ts
● 组件库：native ui（elementPlus体积大了点，考虑到博客用到的组件不会很复杂，并且arco的生态相对差一些，ant的体积也大）
● 动画库：Motion Vue（一定程度上是可以考虑加上anime的）（GSAP的交互相对复杂，性能好，但是博客可能用不到，如果后续要复杂化的话，在引入他去做复杂化的交互就好了）
● 动效库：Vue Bits（InspiraUI的命名有点多了，虽然数量上是高于bits的，但是bits没有外部依赖，轻量）
● 构建：vite
内容
1. 需要一个导航栏，但我希望不是过于泛滥的顶部fixed，可能想把他放在侧边栏，针对每一个板块用一个图片进行代替，鼠标悬停就显示出文字，（但这种没有考虑到移动端，需要想想移动端怎么去显示问题，而不是图片，因为图片可能看不出来是什么意思）
  a. pc：
    ⅰ. 用svg图标+文字标签
    ⅱ. 悬停图标放大+文字显示，添加一些微动效
    ⅲ. 当前激活项高亮
  b. 移动端：
    ⅰ. 可以不适用侧边栏，而是采用底部固定导航栏（汉堡菜单+图标）
    ⅱ. 也可以顶部可折叠导航栏
    ⅲ. 可以用media切换布局
2. 首页开头可以做一个动画啥的（bits）
  a. 写一句话，打字机效果（TextScramble组件）
  b. 加入粒子背景（canvas，鼠标移动进行交互）
  c. 多层背景滚动速度不同，给出深度
  d. 渐变入场，可以考虑从下方划入，再加入透明度的变化
3. 在第二板块就作为我的技能板块，可以考虑用上3D技能树那种，把vue，js，兴趣爱好都放上去（如果可以的话，可以考虑在下面加上一个显示，移动到某一个点的时候，显示出这个点的具体信息）
  a. 可以使用TresJS实现3D
  b. 把技能点作为立方体，球体来放上去
  c. 鼠标悬停放大，显示信息（这里可以考虑信息是从后端获取的，那就要加入管理员模式，自己可以在下面新增，删除）
  d. 使用懒加载（lazy-loading），当视口到技能树区域才加载
  e. 每个作品包含：标题、缩略图、技术栈标签、项目简介
4. 一些作品，可以把4次作品赛的作品都放上去，然后一个卡片翻转或者图片收缩显示文字的这种
  a. 采用css 3D transfrom实现卡片的翻转的话
  b. 悬停的卡片放大
  c. 图片的懒加载，用 intersection observer 让图片按需加载
5. 把协同文档系统放上去，可以考虑是放一个demo，或者iframe插入，（因为后续是要考虑到协同文档系统是要作为子域在博客上的，这里也是要调整的点，但也是部署的时候在说了）
  a. 用子域名部署系统
  b. 博客就通过iframe嵌入或者加入跳转链接
  c. 或者考虑微前端架构（如qiankun）集成
6. 看自己能不能做一些小demo作为作品，放到下面的板块（给了点方向）
  a. canvas动画：粒子系统，3D渲染、物理引擎
  b. 数据可视化：echarts、D3.js
  c. 工具：在线代码编辑、图片处理工具
  d. 简单h5游戏，如果后续还会继续学cocos的话，也可以在这里插入
7. 评论区
  a. 只有登录之后才能评论，没有登录只能对评论进行查看
  b. 每一个评论是可以考虑对这条评论继续评论，并且进行点赞的（这里的评论嵌套是要考虑是采用递归还是多级组件，多级组件逻辑更清楚，但是组件通信可能就麻烦一些，递归是通过判断当前评论是否又replies数组来决定是否递归调用，这个要慎重思考）

如果要加入管理员的话，那其实你就要考虑到每一个板块是不是要留一个区域给管理员进行新增等操作了
作品赛是可以直接放在前端的，毕竟是静态资源